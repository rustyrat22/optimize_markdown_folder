[project]
name = "optimize-markdown-folder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Phil Hampton", email = "hampton.jpjr@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
optimize-markdown-folder = "optimize_markdown_folder:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
